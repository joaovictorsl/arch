-- Programs used throughout the config.
-- Access them from other files with:
--   local programs = require("categories.programs")

local programs = {
	terminal = "ghostty",
	browser  = "zen-browser",
	notes = "obsidian",
	music = "spotify",
	menu     = "rofi -show drun",
}

return programs
